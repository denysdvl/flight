export interface Flight {
  departure: string;
  arrival: string;
  departureKey: string;
  arrivalKey: string;
  departureDate: string;
  departureTime: string;
  arrivalDate: string;
  arrivalTime: string;
}
